import { CodeBlock } from "@/doc-ui/CodeBlock";
import UseTimeAgoSrc from "@/../src/hooks/useTimeAgo.ts?raw";

export const meta = {
  title: "useTimeAgo",
  category: "Hooks",
  order: 4,
};

# useTimeAgo

📁 `src/hooks/useTimeAgo.ts`

Returns a human-readable relative time string and auto-updates every second. Cleans up the interval on unmount.

## Parameters

| Param | Type | Description |
|-------|------|-------------|
| `dateString` | `string` | ISO date string e.g. `"2025-09-24T12:00:00Z"` |

## Returns

`string` — the relative time label. Append `" ago"` yourself.

## Time Ranges

| Elapsed | Output example |
|---------|---------------|
| < 60s | `"45 seconds"` |
| < 60m | `"3 minutes"` |
| < 24h | `"2 hours"` |
| < 30d | `"5 days"` |
| < 12mo | `"3 months"` |
| ≥ 12mo | `"1 year"` |

## Usage

```tsx
const timeAgo = useTimeAgo(post.createdAt);

return <span>{timeAgo} ago</span>;
```

## Source

<CodeBlock title="useTimeAgo.ts" language="ts" source={UseTimeAgoSrc} />
