import { CodeBlock } from "@/doc-ui/CodeBlock";
import NormalizeUrlSrc from "@/../src/utils/normalize-url.utils.ts?raw";

export const meta = {
  title: "Normalize Url",
  category: "Utils",
  order: 11,
};

# normalizeUrl

📁 `src/utils/normalize-url.utils.ts`

Ensures a URL string has an `https://` protocol prefix. Returns the original value if it already starts with `http://` or `https://`. Trims whitespace.

## Examples

```ts
normalizeUrl("example.com");        // "https://example.com"
normalizeUrl("http://example.com"); // "http://example.com"
normalizeUrl("  example.com  ");    // "https://example.com"
normalizeUrl("");                   // ""
```

## Source

<CodeBlock title="normalize-url.utils.ts" language="ts" source={NormalizeUrlSrc} />
