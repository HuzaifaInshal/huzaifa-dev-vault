import { Demo } from "@/doc-ui/Demo";
import { CodeBlock } from "@/doc-ui/CodeBlock";
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from "@/components/ui/Tables";

export const meta = {
  title: "Tables",
  category: "Components",
  order: 5,
};

# Tables

Composable table primitives with consistent border, background, and spacing. The root `Table` wraps in a scrollable container automatically.

📁 `src/components/ui/Tables.tsx`

## Components

| Component | Element | Description |
|-----------|---------|-------------|
| `Table` | `<table>` | Root — overflow scroll wrapper |
| `TableHeader` | `<thead>` | Header with secondary background |
| `TableBody` | `<tbody>` | Body rows |
| `TableRow` | `<tr>` | Row with bottom border |
| `TableHead` | `<th>` | Header cell |
| `TableCell` | `<td>` | Data cell |

All components forward native HTML props and accept a `className` override.

## Live Example

<Demo
  title="Tables"
  code={`import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from "@/components/ui/Tables";

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Name</TableHead>
      <TableHead>Role</TableHead>
      <TableHead>Status</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>Alice</TableCell>
      <TableCell>Admin</TableCell>
      <TableCell>Active</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>Bob</TableCell>
      <TableCell>Editor</TableCell>
      <TableCell>Inactive</TableCell>
    </TableRow>
  </TableBody>
</Table>`}
>
  <div className="w-full max-w-lg">
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>Name</TableHead>
          <TableHead>Role</TableHead>
          <TableHead>Status</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow>
          <TableCell>Alice</TableCell>
          <TableCell>Admin</TableCell>
          <TableCell>Active</TableCell>
        </TableRow>
        <TableRow>
          <TableCell>Bob</TableCell>
          <TableCell>Editor</TableCell>
          <TableCell>Inactive</TableCell>
        </TableRow>
        <TableRow>
          <TableCell>Charlie</TableCell>
          <TableCell>Viewer</TableCell>
          <TableCell>Active</TableCell>
        </TableRow>
      </TableBody>
    </Table>
  </div>
</Demo>

<CodeBlock title="Tables.tsx" language="tsx" source={`import { cn } from "@/lib/utils";
import React from "react";

const Table = ({ className, ...props }: React.ComponentProps<"table">) => (
  <div className="w-full overflow-auto">
    <table className={cn("w-full caption-bottom text-sm", className)} {...props} />
  </div>
);

const TableHeader = ({ className, ...props }: React.ComponentProps<"thead">) => (
  <thead className={cn("bg-background-secondary", className)} {...props} />
);

const TableBody = ({ className, ...props }: React.ComponentProps<"tbody">) => (
  <tbody className={cn("[&_tr:last-child]:border-0", className)} {...props} />
);

const TableRow = ({ className, ...props }: React.ComponentProps<"tr">) => (
  <tr className={cn("border-b border-border-primary bg-background", className)} {...props} />
);

const TableHead = ({ className, ...props }: React.ComponentProps<"th">) => (
  <th className={cn("h-11 px-4 text-left align-middle text-sm font-medium text-text-secondary", className)} {...props} />
);

const TableCell = ({ className, ...props }: React.ComponentProps<"td">) => (
  <td className={cn("h-16 px-4 py-3.5 align-middle", className)} {...props} />
);

export { Table, TableHeader, TableBody, TableRow, TableHead, TableCell };`} />
