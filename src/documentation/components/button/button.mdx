import { Button } from '../../../components/Button'
import buttonSrc from '../../../components/Button.tsx?raw'
import { Demo } from '../../../doc-ui/Demo'

export const meta = {
  title: 'Button',
  category: 'Components',
  order: 1,
}

# Button

A versatile button with multiple variants, sizes, and full keyboard accessibility.

## Default

<Demo source={buttonSrc}>
  <Button>Default Button</Button>
</Demo>

## Variants

<Demo source={buttonSrc}>
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="destructive">Destructive</Button>
</Demo>

## Sizes

<Demo source={buttonSrc}>
  <Button size="sm">Small</Button>
  <Button size="md">Medium</Button>
  <Button size="lg">Large</Button>
</Demo>

## Disabled

<Demo source={buttonSrc}>
  <Button disabled>Disabled</Button>
  <Button variant="secondary" disabled>Disabled</Button>
</Demo>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'primary' \| 'secondary' \| 'ghost' \| 'destructive'` | `'primary'` | Visual style |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Size |
| `disabled` | `boolean` | `false` | Disabled state |
| `className` | `string` | — | Additional classes |

Also accepts all native `<button>` props via spread.
