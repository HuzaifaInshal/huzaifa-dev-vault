/**
 * Global site configuration.
 *
 * SIDEBAR_SCOPED_TO_SECTION
 *   true  → sidebar shows only the children of the active top-nav item
 *   false → sidebar shows every top-level section (original behaviour)
 */
export const SIDEBAR_SCOPED_TO_SECTION = true;
